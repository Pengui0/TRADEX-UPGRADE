# TradeX

A personal trading dashboard for stocks and crypto with real-time simulated market data, charting, and portfolio analytics.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Optionally add `FINNHUB_API_KEY` to your `.env` file for improved US stock coverage.
3. Run the app:
   `npm run dev`
